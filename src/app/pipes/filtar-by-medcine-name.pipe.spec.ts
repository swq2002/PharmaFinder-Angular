import { FiltarByMedcineNamePipe } from './filtar-by-medcine-name.pipe';

describe('FiltarByMedcineNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltarByMedcineNamePipe();
    expect(pipe).toBeTruthy();
  });
});
