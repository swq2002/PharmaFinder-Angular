import { FiltarByPharmacyNamePipe } from './filtar-by-pharmacy-name.pipe';

describe('FiltarByPharmacyNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltarByPharmacyNamePipe();
    expect(pipe).toBeTruthy();
  });
});
