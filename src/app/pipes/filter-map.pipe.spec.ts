import { FilterMapPipe } from './filter-map.pipe';

describe('FilterMapPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMapPipe();
    expect(pipe).toBeTruthy();
  });
});
